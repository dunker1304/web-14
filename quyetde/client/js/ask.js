$(document).ready(function() {
  $('#questionInput').on("input", function() {
    $('#count').text($('#questionInput').attr('maxlength') - $('#questionInput').val().length);
  });

  $('#askForm').on('submit', function(e) {
    e.preventDefault();
  });
empty()
html('')
  `${dsada}
  saddas<a href="http://${asdas}">
  sdasadsa
  adsdsadsa${dsada}
  sad`
});